# Core — Django 5.1 supports Python 3.10–3.13
Django==5.1.6
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.7.0
python-decouple==3.8
gunicorn==23.0.0

# MongoDB
mongoengine==0.29.1
pymongo==4.10.1

# Redis + Caching
redis==5.2.1
django-redis==5.4.0
# hiredis is optional (C extension, speeds up Redis parsing — skip if build fails)
# hiredis==3.0.0

# Celery
celery==5.4.0
django-celery-beat==2.7.0
django-celery-results==2.5.1

# Auth
bcrypt==4.2.1

# File storage
Pillow==11.1.0

# Filtering & pagination
django-filter==24.3

# Utilities
python-dateutil==2.9.0
